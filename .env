VITE_OTEL_EXPORTER=OTLP
# Persistence for mock appointments + pending: json | localstorage
# json         → .runtime-data/*.json via Vite /__runtime (npm run dev)
# localstorage → browser localStorage (also works with preview / static host)
VITE_LOCAL_SAVE_MODE=localstorage
